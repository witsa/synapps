{
  "name": "synapps-doc",
  "scripts": {
    "serve": "bundle exec jekyll serve --config ./_config.yml,./_config_dev.yml --safe",
    "build": "rm .jekyll-metadata && bundle exec jekyll build --config ./_config.yml,./_config_dev.yml --safe "
  }
}
