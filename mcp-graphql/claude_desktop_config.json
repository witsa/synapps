{
  "mcpServers": {
    "graphql-mcp": {
      "command": "python",
      "args": [
        "<<path_to_mcp_graphql_server.py>>" 
      ],
      "description": "A simple MCP server to query documents from SQL Server database in Microsoft Fabric  using Fabric GraphQL."
    }
  }
}